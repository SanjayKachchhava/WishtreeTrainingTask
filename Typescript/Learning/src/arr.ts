let numbers : number[] = [1, 2, 3, 4, 5, 6];


numbers.forEach((number) => {
    number.toFixed(2);
});

// tuple is fixed length array with specified types
let tuple : [number,string] = [1, "sanjay"];


